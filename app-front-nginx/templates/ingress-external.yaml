{{- if .Values.ingress.external.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  {{- if .Values.ingress.external.annotations }}
  annotations:
    {{- toYaml .Values.ingress.external.annotations | nindent 4 }}
  {{- end }}
  name: {{ required "A value for 'name' is required!" .Values.name }}-ingress
  namespace: {{ .Values.namespace }}
spec:
  ingressClassName: {{ .Values.ingress.external.ingressClassName | default "nginx" }}
  rules:
  {{- $hosts := .Values.ingress.external.hosts | default (list .Values.ingress.external.host) }}
  {{- range $hosts }}
  - host: {{ . }}
    http:
      paths:
      - backend:
          service:
            name: {{ required "A value for 'name' is required!" $.Values.name }}-service
            port:
              name: {{ $.Values.service.portName | default "http" }}
        path: /
        pathType: Prefix
  {{- end }}
  tls:
  {{- if .Values.ingress.external.tls.configs }}
  {{- range .Values.ingress.external.tls.configs }}
  - hosts:
    {{- range .hosts }}
    - {{ . }}
    {{- end }}
    secretName: {{ .secretName }}
  {{- end }}
  {{- else }}
  - hosts:
    {{- range $hosts }}
    - {{ . }}
    {{- end }}
    secretName: {{ required "ingress.*.tls.secretCertificate is required when tls.configs is not set!" .Values.ingress.external.tls.secretCertificate  }}
  {{- end }}
{{- end }}
