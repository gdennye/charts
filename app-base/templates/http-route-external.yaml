{{- if $.Values.ingress.external.enabled }}
{{- $hosts := .Values.ingress.external.hosts | default (list .Values.ingress.external.host) }}
{{- range $index, $host := $hosts }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ required "A value for 'name' is required!" $.Values.name }}-{{ $index }}-http-route-e
  namespace: {{ $.Values.namespace }}
spec:
  parentRefs:
  {{- if $.Values.ingress.external.sectionNames }}
  {{- range $.Values.ingress.external.sectionNames }}
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: gateway-external
    namespace: gateway-external
    sectionName: {{ . }}
  {{- end }}
  {{- else }}
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: gateway-external
    namespace: gateway-external
  {{- end }}
  hostnames:
  - {{ $host | quote }}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - group: ''
      kind: Service
      name: {{ required "A value for 'name' is required!" $.Values.name }}-service
      port: {{ $.Values.service.ports.port | default 80 }}
      weight: 1
{{- end }}
{{- end }}